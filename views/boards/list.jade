extends ../layout

block container
  #app.front
    .start
      form(method="post", action="/boards")
        input(type="hidden", name="board_size", value=9)
        input(id="small-game-button", type="submit", value="start a small game")
 
      h1 Weiqi

      form(method="post", action="/boards")
        input(type="hidden", name="board_size", value=19)
        input(id="big-game-button", type="submit", value="start a big game")
